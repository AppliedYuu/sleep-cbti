# 睡眠良方（sleep-cbti）上线部署方案对比与选型建议

> 文档目的：帮助你在已有**腾讯云独立域名**的前提下，用**最具性价比**的平台把这个项目全部部署上线。
> 状态：仅方案对比，未执行任何部署操作。

---

## 一、项目架构与"上线必备清单"

### 1.1 当前架构
- **技术栈**：Node.js (Express 5) 后端 + MySQL (mysql2) 数据库 + Vue 3 前端。
- **关键设计**：后端 `server/src/app.js` 用 `express.static(clientDist)` **同时托管前端静态文件**——即**前后端天然同源**（一体式部署）。
- **现有部署文件**：
  - `railway.json`：Railway 平台配置（Nixpacks 构建）。
  - `.github/workflows/deploy.yml`：GitHub Pages 静态部署 `docs/`。

### 1.2 距离"全部上线"缺什么（清单）
| # | 环节 | 必需? | 说明 |
|---|---|---|---|
| 1 | **后端运行时**（Node 服务） | 必需 | 跑 `server/`，需要长期在线的服务器/容器 |
| 2 | **数据库**（MySQL） | 必需 | 全量数据存储，需长期在线 |
| 3 | **独立域名 + DNS 解析** | 必需 | 已有腾讯云域名，需解析到部署服务 |
| 4 | **HTTPS 证书** | 必需 | 腾讯云免费 SSL 或平台自动证书 |
| 5 | **前端静态托管** | 可选可省 | 同源让后端托管即省去（推荐） |
| 6 | **CORS 白名单更新** | 必须 | 目前写死 `AppliedYuu.github.io`，需加入你的域名 |
| 7 | **对象存储**（上传图片等） | 暂不需要 | 当前无用户上传，可省 |

> ⚠️ **必做项（无论选哪个平台）**：把 `https://你的域名` 加入 `server/src/app.js` 的 CORS 白名单，否则浏览器会拦截跨域请求。当前文件里是：
> `'https://AppliedYuu.github.io'` ← 需改成/追加你的腾讯云域名。

---

## 二、平台方案对比

### 方案 A：腾讯云轻量应用服务器（自建）⭐ 最便宜、最可控
| 项 | 选型 | 参考价 |
|---|---|---|
| 后端 Node | 腾讯云**轻量应用服务器**（2核2G 含带宽） | 约 ¥50–100/月（新用户常有低价） |
| 数据库 | 同机装 MariaDB/MySQL | ¥0（含在服务器） |
| 前端 | 后端同源托管 | ¥0 |
| 域名/HTTPS | 已购域名 + 腾讯云免费 SSL | ¥0 |

**优点**：单机全搞定，前后端同源无 CORS 烦恼；不依赖托管平台涨价/休眠；完全掌控。
**缺点**：需自己运维（装环境、部署、备份、安全更新）。

### 方案 B：免费 PaaS（Railway/Render）+ 托管数据库 ⭐ 最省钱、最省事
| 项 | 选型 | 参考价 |
|---|---|---|
| 后端 Node | Railway（已有 `railway.json`）或 Render | Free 或 ~$5/月 |
| 数据库 | Railway MySQL / Render | Free ~ $7/月 |
| 前端 | 后端同源 或 GitHub Pages | ¥0 |
| 域名/HTTPS | 平台绑定域名 + 自动证书 | ¥0 |

**优点**：git push 即部署、免运维；`railway.json` 已配好，切换成本低。
**缺点**：**免费实例会休眠**（你已遇到过 Railway 数据库休眠连不上）；免费额度有限；域名绑定某些平台需付费版本才支持自定义域名。

### 方案 C：全腾讯云托管（云数据库 + 云托管/CloudBase）⭐ 一体化、最省心、中文生态好
| 项 | 选型 | 参考价 |
|---|---|---|
| 后端 Node | 腾讯云**云托管** / CloudBase / 轻量应用 | 按量或包月 |
| 数据库 | 腾讯云**云数据库 MySQL 基础版** | 新人活动常 ¥几十/年 |
| 前端 | 后端同源 或 COS 静态网站托管 | COS 几乎免费 |
| 域名/HTTPS | 已购域名 + 免费 SSL | ¥0 |

**优点**：域名、证书、数据库、服务器全在腾讯云，DNS/HTTPS/备案一体化最顺；中文文档与客服；稳定性高。
**缺点**：无优惠时标准规格比国外平台略贵。

---

## 三、性价比结论（结合你已持腾讯云独立域名）

| 需求侧重 | 推荐 | 核心理由 |
|---|---|---|
| **预算最省 / 免费** | 方案 B（Railway Free + 后端同源托管） | 几乎零成本，`railway.json` 已备好 |
| **成本与可控平衡** | 方案 A（腾讯云轻量服务器） | 单机一体化，长期最便宜且稳定 |
| **最省心 / 中文生态** | 方案 C（全腾讯云） | 域名/数据库/服务在同一生态，运维最简 |
| **纯个人学习、零预算** | GitHub Pages + Railway(后端) | 前端 Pages 免费、后端 Railway 免费 |

**总体建议**：
- 若想**最省钱**且能接受一定运维 → **方案 A（腾讯云轻量 2核2G）**。
- 若想**几乎零成本 + 免运维**、能接受实例休眠 → **方案 B（Railway Free）**。
- 若预算宽裕、**看重省心与一体化** → **方案 C（全腾讯云）**。
- 由于你**已有腾讯云域名**，无论选谁都要做域名解析 + HTTPS + CORS 白名单三步。

---

## 四、上线第二步（任何方案都需执行的必做项）

1. **更新 CORS 白名单**：`server/src/app.js` → 把 `https://你的域名` 加入 `defaultOrigins`（或通过 `CORS_ORIGINS` 环境变量注入，不必改代码）。
2. **确认前端 API 走同源**：`client/public/api-config.json` 保持 `{"apiBase": "/api"}`（已改好），`request.js` 默认 `/api`（已改好）。
3. **域名解析**：在腾讯云 DNS 添加记录指向所选平台/服务器 IP（A 记录）或 CNAME。
4. **配置 HTTPS**：腾讯云 SSL 证书绑定域名，或开启平台自动证书。
5. **初始化数据库**：执行 `server/src/config/init.sql`（目标库建表）。
6. **设置密钥**：`JWT_SECRET`、`DATABASE_URL` 等一律用平台环境变量/Secret 注入，勿写死提交。

---

## 五、风险与注意点

- **免费实例休眠**：Railway/Render 免费实例长时间无访问会休眠，影响可用性；生产建议最低付费档或方案 A/C 常驻。
- **CORS 域名写死**：换域名后必须同步更新白名单，否则浏览器报跨域错误。
- **数据库长期在线**：本地 XAMPP/开发库仅供测试；上线必须用云端 MySQL 并备份。
- **密钥泄露**：你之前 `render.yaml`/`.env` 出现过明文密钥，上线务必轮换并走 Secret 注入。
- **GitHub Pages 与后端跨域**：若前端放 GitHub Pages、后端在云端不同域，`/api` 会 404/跨域；推荐前端由后端同源托管。

---

*本文档由当前项目的实际配置审查得出，可按需修改后作为你的上线决策参考。*
